# Public site URL (no trailing slash) — canonical URLs, Open Graph, Twitter cards.
# Copy to `.env` for local dev and CI; production hosts often set env vars in the dashboard instead.
VITE_SITE_URL=https://samuelbchua.ca
